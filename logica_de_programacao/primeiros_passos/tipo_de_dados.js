// Declara uma variável chamada 'idade' e atribui o valor inteiro 30
let idade = 30;

// Declara uma variável chamada 'preco' e atribui um número com ponto flutuante (decimal)
let preco = 99.99;

// Exibe no console uma mensagem identificando um número inteiro e o valor da variável 'idade'
console.log("Numero (inteiro): ", idade);

// Exibe no console uma mensagem identificando um número decimal e o valor da variável 'preco'
console.log("Número (ponto flutuante): ", preco);

// Declara uma variável chamada 'nome' e atribui uma string (texto)
let nome = "felipe";

// Declara uma variável chamada 'saudacao' e atribui uma string com uma mensagem
let saudacao = "ola mundo!";

// Exibe no console o texto "String" seguido do valor da variável 'nome'
console.log("String:", nome);

// Exibe no console o texto "String:" seguido do valor da variável 'saudacao'
console.log("String:", saudacao);

// Declara uma variável booleana chamada 'estalogado' com valor verdadeiro (true)
let estalogado = true;

// Declara uma variável booleana chamada 'maiordeidade' com valor falso (false)
let maiordeidade = false;

// Exibe no console uma mensagem indicando um valor booleano verdadeiro
console.log("Boolean (verdadeiro):", estalogado);

// Exibe no console uma mensagem indicando se é maior de idade (valor booleano)
console.log("Maior de idade:", maiordeidade);
